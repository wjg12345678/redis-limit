apiVersion: 1

providers:
  - name: redis-rate-limiter
    orgId: 1
    folder: Redis Rate Limiter
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards/json
